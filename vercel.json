{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "corepack enable && corepack prepare pnpm@11.0.8 --activate && cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @getx/database db:generate",
        "buildCommand": "next build"
      }
    }
  ]
}
