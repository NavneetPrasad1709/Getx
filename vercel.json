{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "corepack enable && cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @getx/database db:generate",
        "buildCommand": "next build"
      }
    }
  ]
}
